name: drinkaholic
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  permission_handler: ^12.0.1
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  uuid: ^4.2.2
  shared_preferences: ^2.2.2
  flutter_screenutil: ^5.9.3

  # ANADIR — SQLite
  sqflite: ^2.3.3
  path: ^1.9.0

  # ANADIR — Compartir y QR
  share_plus: ^10.0.0
  qr_flutter: ^4.1.0
  archive: ^3.6.1
  mobile_scanner: ^5.2.0

  # Firebase — configurado con google-services.json
  firebase_core: ^3.11.0
  cloud_firestore: ^5.6.3
  firebase_crashlytics: ^4.1.0
  firebase_analytics: ^11.3.0

  # ANADIR — AdMob + UMP SDK (GDPR)
  google_mobile_ads: ^5.3.0
  sqflite_common_ffi_web: ^1.0.2
  google_fonts: ^8.0.2
  url_launcher: ^6.3.2
  firebase_app_check: ^0.3.2+10
  firebase_auth: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/drinkaholic_logo.png
    - assets/images/drinkaholic_logo.webp
    - assets/images/logo.webp
    - assets/images/promo.webp
    - assets/images/promo2.webp
    - assets/
    - assets/avatars/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/drinkaholic_logo.png"
  # Configuración para iconos adaptativos en Android
  adaptive_icon_background: "#1a0a2e"  # Fondo oscuro morado del logo
  adaptive_icon_foreground: "assets/images/drinkaholic_logo.png"  # Logo en primer plano
  # También generar iconos tradicionales
  min_sdk_android: 21
  # Eliminar canal alfa para iOS (requerido por App Store)
  remove_alpha_ios: true
  # Remover padding para iconos adaptativos
  adaptive_icon_padding: 0

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
